services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: redid
      POSTGRES_USER: redid
      POSTGRES_PASSWORD: redid
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U redid -d redid"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
